You can adjust the program language in Menu→选项→设置.\n你可以在菜单→选项→设置中调整程序语言。
在绘制连接地形时，按PageUp/PageDown可调整地形放置高度。
支持按住Alt键直线放置单元标记。
您可以通过快捷键F1快速保存地图，不会弹出任何对话框。
在触发编辑器中，支持将行为列表中的行为拖拽至多开编辑器。
您可以为路径点和单元标记自定义颜色，便于区分。
按住Ctrl拖动单位，可快速更改其朝向，支持32向面向。
可以在FAData.ini中为建筑、步兵等设置默认属性。
支持将地图渲染为图片，并可批量处理多张地图。
通过Lua脚本，您可以读取和修改地图数据，实现自动化操作。
您可以使用Lua脚本终端执行脚本刷，实现批量操作。
在地图注释中，您可以为触发、小队等添加备注信息。
支持读取Ra2CsfToolsGUI导出的ini文件作为CSF文本。
在多选模式下，您可以隐藏已选单元格，便于观察。
支持将地图文件关联至启动器，双击即可打开。
您可以为VXL单位设置自定义光源方向，调整渲染效果。
支持显示隐形单位为半透明。
您可以使用距离标尺测量地图上两点之间的距离。
支持将多选区域的地形批量填充为指定类型。
您可以在物品浏览器中预览所有单位、地形的缩略图。
您可以使用全局搜索快速定位单位、路径点或坐标。
支持为每种地图类型单独设置忽略的覆盖物或单位。
支持将地形组预设保存为presets_XXX.map文件，便于复用。
支持在保存地图时保留注释和INI小节顺序。
您可以为每个地图类型单独设置大水面和小水面索引。
您可以为围墙覆盖物设置所属色，依据最近建筑显示。
支持将步兵精确放置在地形的子单元格中。
您可以使用地形生成器基于柏林噪声随机生成地形。
支持为VXL单位的炮塔和炮管渲染阴影。
您可以使用快捷键Ctrl+Shift+O快速重载当前地图。
支持为多人地图设置Phobos <Player @ X>风格的玩家所属方。
您可以为地图添加注释，并双击快速编辑内容。
可以用笔刷批量更改地图中单位的所属方。
通过ConnectedTileDrawer.ini自定义连接地形的类型和规则。
通过TileConvertRules.ini配置跨地形粘贴时的自动转换规则。
利用FADialog.ini翻译FA2的任意窗口文本，打造完全本地化的界面。
将自定义的target.bmp、celltag.bmp等图像放入[pics]文件夹，替换默认图标。
崩溃时地图会自动备份到CrashBackups文件夹，文件名包含时间戳。
物品浏览器缩略图缓存于thumbnails文件夹，首次生成后加载更快。
将高优先级资源放入Resources\HighPriority文件夹，可优先加载。
通过[NestedMixes]配置嵌套MIX文件的加载顺序，灵活管理资源包。
在[ExtraPackages]中设置加密资源包，保护您的原创素材。
使用[ExtraStringtables]加载额外的CSF、LLF、ECS字符串表。
通过[LATGroups]和[LATSettings]自定义新的LAT地形，支持MOD扩展。
在[BrushSizes]中自定义笔刷大小列表。
通过[AutoShoreTypes]为每种地图类型配置多种自动海岸，实时切换。
使用[SoftTileSetsXXX]控制自动海岸是否覆盖特定地形组。
通过[RedrawExtraTileSetsXXX]指定需要二次绘制ExtraImage的地形组。
利用[NoExtraHeightRedrawTileSetsXXX]防止特定地形遮挡高架桥覆盖物。
在[ObjectBrowser.Overlays]中对覆盖物进行分类，便于查找。
通过[IgnoreOverlays]屏蔽不需要显示的覆盖物，简化物品浏览器。
使用[RenameID]强制翻译指定ID的游戏对象。
通过[MapRendererIgnoreObjects]在地图渲染器中忽略某些单位或覆盖物。
在[PlaceRandomTileList]等小节中注册随机摆放预设。
当地图文件位于不同游戏目录时，FA2会提示是否重新加载对应资源。
在下拉菜单中输入文字可快速搜索，支持*和|通配符。
使用“查找引用”功能快速定位哪些触发、小队引用了当前对象。
通过“自动计算LAT”一键修复全图地形连接，确保与游戏内逻辑一致。
使用“重绘水面”功能修复不完整的水面，让水体更自然。
打开“不可通行单元”图层，直观查看哪些格子单位无法到达。
通过图层筛选功能，只显示特定类型的对象，减少干扰。
在地形生成器中，可直接使用多选区域作为生成范围，无需手动设置。
通过fa2extra_前缀的INI文件覆盖游戏原有设置。
使用[ForceSides]将同一科技类型放入多个物品浏览器分组。
摆放围墙时可以选择不同破损程度，系统会自动连接相邻围墙。
按Delete键快速调出删除对象。
地图顶部细蓝线表示游戏内实际可达的上边界。
粘贴选项可选择仅粘贴地形、覆盖物等特定类型。
从BMP导入地图时，可指定生成的地图类型（如 Temperate）。
放置建筑时会自动检查重叠，并可选择是否强制放置。
按住Shift可进行陡峭抬升，生成两格高度的高地。
按住Shift+Ctrl可强制抬升，即使地形不可抬升也会生成斜坡。
连续放置地形或覆盖物时，多次操作合并为一个撤销步骤。
在多人地图中，可用路径点从8开始，避免与游戏者位置冲突。
使用“删除斜坡上的矿石”Lua脚本，快速移除斜坡上的不合理矿石。
通过“随机矿石图像”Lua脚本，让矿石外观更加多样。
当下拉菜单、列表中元素数量超过SearchCombobox.MaxCount（默认为1000）时，需要按下Enter才能执行搜索